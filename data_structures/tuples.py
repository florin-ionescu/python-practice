x = 5 , 6 # This is a tuple, not a list. A tuple is immutable, meaning it cannot be changed after it is created.
print(type(x))

y = (5, 6) # This is also a tuple. The parentheses are optional when defining a tuple, but they can be used for clarity.
print(type(y))